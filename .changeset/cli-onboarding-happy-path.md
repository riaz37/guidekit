---
"@guidekit/cli": patch
---

Scaffold full proxy-mode routes (`lib/guidekit-routes.ts`, token/llm/health API routes) on `guidekit init` for Next.js App Router. Extend `guidekit doctor` with integration and local endpoint checks.
